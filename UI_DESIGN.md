# 文档助手 ChatBot — UI 设计文档

**版本**：v1.0  
**日期**：2026-06-06  
**风格基调**：AI-Native UI · 深色极简 · 高效工具型

---

## 一、设计哲学

### 核心原则

**1. 功能即界面**  
文档整理是核心目的。界面的每一个元素都服务于"上传 → 选择 → 提问 → 获得结果"这条主线，没有多余的装饰。

**2. 状态永远清晰**  
用户始终知道：文件在不在、解析好没好、AI 在干什么、结果在哪里。不让用户猜。

**3. 等待有反馈，过程可感知**  
参考 Claude Code 的步骤式反馈，AI 处理过程中逐步展示正在执行的动作，而不是一个转圈等待。

**4. 克制的视觉层级**  
深色背景 + 三层灰度 + 单一强调色。颜色只用于传递信息，不做装饰。

---

## 二、颜色系统

### 基础色板

| Token | Hex | 用途 |
|-------|-----|------|
| `bg-base` | `#0A0A0A` | 最底层背景（整体页面） |
| `bg-sidebar` | `#111111` | 侧栏背景 |
| `bg-surface` | `#1A1A1A` | 卡片、气泡、浮层 |
| `bg-elevated` | `#222222` | hover 高亮、输入框 |
| `border-subtle` | `#2A2A2A` | 分割线、边框 |
| `border-default` | `#3A3A3A` | 常规边框 |

### 文字色

| Token | Hex | 用途 |
|-------|-----|------|
| `text-primary` | `#F5F5F5` | 主要文字、标题 |
| `text-secondary` | `#A0A0A0` | 次要文字、说明 |
| `text-muted` | `#555555` | 占位符、禁用文字 |

### 强调色（蓝色系）

| Token | Hex | 用途 |
|-------|-----|------|
| `accent` | `#2563EB` | 主强调色：按钮、选中框、链接 |
| `accent-hover` | `#3B82F6` | 悬停态 |
| `accent-subtle` | `rgba(37,99,235,0.12)` | 选中背景、高亮区域 |
| `accent-border` | `rgba(37,99,235,0.35)` | 选中边框 |

### 状态色

| Token | Hex | 用途 |
|-------|-----|------|
| `status-pending` | `#F59E0B` | 解析中（黄色） |
| `status-ready` | `#10B981` | 已就绪（绿色） |
| `status-error` | `#EF4444` | 解析失败（红色） |
| `status-pending-bg` | `rgba(245,158,11,0.10)` | 解析中背景 |
| `status-ready-bg` | `rgba(16,185,129,0.10)` | 就绪背景 |
| `status-error-bg` | `rgba(239,68,68,0.10)` | 失败背景 |

---

## 三、字体系统

**字体**：Inter（全栈统一）  
```
@import url('https://fonts.google.com/css2?family=Inter:wght@300;400;500;600;700&display=swap');
```

| 层级 | 大小 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| 标题 | 15px | 600 | 1.4 | 模块标题（文件管理、对话） |
| 正文 | 14px | 400 | 1.6 | 消息内容、文件名 |
| 辅助 | 13px | 400 | 1.5 | 文件大小、时间、状态 |
| 标签 | 12px | 500 | 1.4 | 徽章、按钮文字 |
| 代码 | 13px | 400 | 1.6 | Markdown 代码块（JetBrains Mono） |

---

## 四、整体布局

```
┌──────────────────────────────────────────────────────────────────┐
│  Header  [≡]  文档助手                                    (48px) │
├────────────────┬─────────────────────────────────────────────────┤
│                │                                                  │
│   侧栏         │   聊天区域                                       │
│   (272px)      │   (flex-1)                                       │
│                │                                                  │
│  ┌──────────┐  │  ┌────────────────────────────────────────────┐ │
│  │ 上传区域 │  │  │  消息列表（可滚动）                         │ │
│  └──────────┘  │  │                                            │ │
│                │  │                                            │ │
│  文件列表      │  │                                            │ │
│  (可滚动)      │  └────────────────────────────────────────────┘ │
│                │  ┌────────────────────────────────────────────┐ │
│                │  │  输入区域                          (auto)  │ │
│                │  └────────────────────────────────────────────┘ │
└────────────────┴─────────────────────────────────────────────────┘
```

- **整体高度**：`100vh`，无纵向滚动条
- **侧栏宽度**：272px 固定，可通过 Header 按钮折叠（动画 `transition: width 250ms ease`）
- **折叠后**：`width: 0`，内容 `overflow: hidden`，聊天区域自动扩展
- **分割线**：`1px solid #2A2A2A`

---

## 五、Header

```
┌──────────────────────────────────────────────────────────────────┐
│  [侧栏折叠按钮]  [Bot图标]  文档助手                              │
└──────────────────────────────────────────────────────────────────┘
```

- **高度**：48px
- **背景**：`#0A0A0A`，底部 `1px solid #2A2A2A`
- **侧栏折叠按钮**：`PanelLeftClose` / `PanelLeft` 图标，`p-6`，hover `bg-elevated` 圆角 6px
- **Bot 图标**：`#2563EB`，16px
- **标题**：`text-primary`，14px，500 字重

---

## 六、侧栏 — 文件管理

### 6.1 整体结构

```
┌──────────────────────────┐
│  文件管理   [N 个文件]   │  ← 标题行
├──────────────────────────┤
│  ┌────────────────────┐  │
│  │   拖拽上传区域      │  │  ← UploadZone
│  └────────────────────┘  │
├──────────────────────────┤
│  已选 N 个      [全选▢] │  ← 选择控制行
├──────────────────────────┤
│  文件列表（可滚动）       │
│  ...                     │
└──────────────────────────┘
```

### 6.2 上传区域（UploadZone）

- **尺寸**：`mx-3 mt-3`，高度约 72px
- **外观**：`border-2 border-dashed border-[#3A3A3A]`，圆角 10px，背景透明
- **内容**：上传图标（16px，`text-muted`）+ "拖拽或点击上传"（12px）+ "PDF · DOCX · 图片"（11px，`text-muted`）
- **拖入激活态**：`border-accent bg-accent-subtle`，文字变 `#3B82F6`，过渡 150ms
- **上传中**：图标替换为旋转 `Loader2`（蓝色），文字"上传中..."
- **粘贴响应**：在输入框内粘贴文件时，UploadZone 短暂高亮（400ms flash）提示文件已接收

### 6.3 文件列表项（FileItem）

```
┌─────────────────────────────────────────────┐
│ [☑] [📄] report.pdf             [✏][↓][🗑] │
│      已就绪 · 1.2 MB                        │
└─────────────────────────────────────────────┘
```

**布局**：`flex items-start gap-2.5 px-3 py-2.5 mx-2 rounded-xl`

| 元素 | 规格 |
|------|------|
| 复选框 | 16×16px，选中 `bg-accent border-accent`，内有白色 `Check` 10px |
| 文件图标 | 16px，PDF=红 `#EF4444`，DOCX=蓝 `#3B82F6`，图片=紫 `#A855F7` |
| 文件名 | 14px，`text-primary`，超长 truncate，最大 1 行 |
| 解析状态 | 见 6.4 |
| 操作按钮 | `opacity-0 group-hover:opacity-100`，过渡 150ms |

**选中态**：`bg-accent-subtle border border-accent-border`  
**默认态**：`hover:bg-[#1E1E1E] border border-transparent`  
**整行可点击**（切换选中）

**重命名态**：文件名替换为 Input，`bg-[#0A0A0A] border border-[#3A3A3A]`，右侧 `✓` / `✗` 按钮

### 6.4 解析状态徽章

位于文件名下方，`flex items-center gap-1`：

| 状态 | 图标 | 文字 | 颜色 |
|------|------|------|------|
| 解析中 | `Loader2`（旋转，12px） | 解析中... | `#F59E0B` |
| 已就绪 | `CheckCircle`（12px） | 已就绪 | `#10B981` |
| 解析失败 | `AlertCircle`（12px） | 解析失败 | `#EF4444` |

文字 11px，图标和文字同色。

---

## 七、聊天区域

### 7.1 空状态引导页

当无消息时，聊天区域中央显示：

```
        ┌────────────────┐
        │   [Bot 图标]   │   56×56px 圆角 16px，bg-surface
        └────────────────┘
         文档分析助手          16px 600 text-primary
    在左侧上传文件并勾选，       14px text-secondary 居中
    然后发送消息开始分析。       最大宽度 260px，行高 1.6
```

### 7.2 消息气泡

**用户消息（右对齐）**
```
                              [User] 帮我总结这份报告
                                     ─────────────────
                                     bg: #2563EB
                                     圆角: 16px 16px 4px 16px
                                     最大宽度: 75%
                                     padding: 10px 14px
```

**AI 消息（左对齐）**
```
[Bot] 以下是文件的主要内容：
      ## 摘要
      ...
      ─────────────────────────────
      bg: #1A1A1A
      圆角: 16px 16px 16px 4px
      最大宽度: 85%
      padding: 12px 16px
      Markdown 渲染区
```

**消息间距**：`space-y-6`，同角色连续消息间距 `space-y-2`  
**头像**：32×32px 圆形，用户=蓝底白色 User 图标，AI=灰底白色 Bot 图标

**AI 消息操作**（hover 后气泡下方出现，`opacity-0 → opacity-100`，150ms）：

```
[复制]  [导出]
```

### 7.3 Markdown 渲染规范

| 元素 | 样式 |
|------|------|
| h1 | 20px，600，`text-primary`，mb-3 |
| h2 | 17px，600，`text-primary`，mb-2 |
| h3 | 15px，600，`#D1D5DB`，mb-1.5 |
| p | 14px，400，`#D1D5DB`，行高 1.7，mb-3 |
| code（行内） | 13px，`bg-[#111]`，`#93C5FD`，px-1.5 rounded-md |
| pre（代码块） | `bg-[#0D0D0D] border border-[#2A2A2A]`，圆角 8px，左上角显示语言标签 |
| blockquote | 左边 `2px solid #2563EB`，`text-secondary`，pl-3 |
| table | 边框 `#2A2A2A`，th `bg-surface`，交替行背景 `#141414` |
| ul/ol | `text-[#D1D5DB]`，`pl-4`，`space-y-1` |
| 分割线 | `border-[#2A2A2A]` |

---

## 八、AI 处理反馈（核心设计）

这是本产品体验的关键差异点。参考 Claude Code 的工具调用展示方式，在 AI 响应开始流式输出之前，展示一个**步骤式进度面板**。

### 8.1 触发时机

用户点击发送 → 请求到达后端 → **步骤面板出现** → 逐步更新状态 → 内容开始流式输出 → 步骤面板收起（折叠动画）→ 流式文字显示

### 8.2 步骤面板外观

```
┌─────────────────────────────────────────────┐
│  ⠿  正在处理...                              │  ← 顶部标题行（灰色）
├─────────────────────────────────────────────┤
│  ✓  读取文件                    report.pdf  │  ← 已完成步骤（绿色）
│  ✓  提取文档内容                3,420 字    │  ← 已完成步骤（绿色）
│  ⟳  分析文档结构...                         │  ← 当前执行步骤（蓝色+旋转）
│  ·  生成回答                                │  ← 待执行步骤（灰色，虚化）
└─────────────────────────────────────────────┘
```

**面板规格**：
- 背景：`#141414`，`border border-[#2A2A2A]`，圆角 12px
- `padding: 12px 14px`
- 宽度：跟随 AI 气泡最大宽度（85%）
- 出现动画：从下方 8px + fade in，`200ms ease-out`

**步骤行**（`flex items-center gap-2.5 py-1.5`）：

| 状态 | 图标 | 图标颜色 | 文字颜色 |
|------|------|----------|----------|
| 待执行 | `Circle`（12px，虚线感） | `#444` | `#555`（muted） |
| 执行中 | `Loader2`（12px，旋转） | `#2563EB` | `#F5F5F5` |
| 已完成 | `CheckCircle`（12px） | `#10B981` | `#A0A0A0` |

**右侧附加信息**（`ml-auto text-[11px] text-muted`）：文件名、字数、模型名等上下文

### 8.3 步骤内容定义

根据不同场景展示不同步骤：

**场景 A：有文件上下文**
```
1. 读取文件         [文件名列表]
2. 提取文档内容     [X 字]
3. 分析文档结构
4. 生成回答
```

**场景 B：无文件，纯对话**
```
1. 理解问题
2. 生成回答
```

**场景 C：文件解析失败**
```
1. ✕ 读取文件失败   [文件名]（红色错误行）
2. 生成回答         （跳过文件，直接作答）
```

### 8.4 步骤推进逻辑

- 步骤间切换间隔：模拟节奏感，每步骤激活延迟 **400-800ms**（前端模拟，不依赖后端真实进度）
- 当后端 SSE 第一个 `content` chunk 到达时：步骤面板执行"折叠收起"动画（`height: 0`，`opacity: 0`，`200ms ease-in`），然后流式文字开始出现

### 8.5 流式光标

流式输出期间，当前文字末尾显示：

```css
/* 闪烁光标 */
display: inline-block;
width: 2px;
height: 14px;
background: #2563EB;
margin-left: 2px;
vertical-align: middle;
animation: blink 0.8s step-end infinite;
```

---

## 九、聊天输入区

```
┌──────────────────────────────────────────────────────┐
│  已选 2 个文件作为上下文                              │  ← 上下文提示（有选中时显示）
├──────────────────────────────────────────────────────┤
│  [📎]  发送消息... (Shift+Enter 换行)        [发送▶] │
└──────────────────────────────────────────────────────┘
│  AI 可能犯错，请核实重要信息                          │  ← 免责声明（居中 12px muted）
```

**输入框容器**：
- 背景：`#1A1A1A`，`border border-[#2A2A2A]`，圆角 16px
- `focus-within`: `border-[#3A3A3A]`，过渡 150ms
- `padding: 8px 12px`

**文字输入区（textarea）**：
- 自适应高度，最小 1 行，最大 6 行（160px）
- 背景透明，无边框，14px，`text-primary`
- placeholder `text-muted`

**附件按钮**：
- `Paperclip` 图标，18px，`text-muted hover:text-secondary`，`p-1.5 rounded-lg hover:bg-elevated`

**发送按钮**：
- 有内容时：`bg-accent hover:bg-accent-hover`，白色 `Send` 图标
- 无内容 / 流式中：`bg-[#1E1E1E] text-muted`，禁用态
- 流式输出中：显示旋转 `Loader2`（蓝色），不可点击

**上下文提示条**（有选中文件时显示）：
```
已选 2 个文件作为上下文
```
- `bg-[rgba(37,99,235,0.08)]`，`border border-accent-border`，圆角 20px（pill 形）
- 12px，`#3B82F6`，`px-3 py-1`
- 动画：fade + slide-down 出现（150ms）

**拖拽进入输入区**：整个输入框区域出现蓝色边框高亮 + 遮罩文字"松开上传文件"

---

## 十、导出弹窗

```
┌────────────────────────────────┐
│  导出内容              [✕]    │
├────────────────────────────────┤
│  导出范围                      │
│  ● 仅此条消息                  │
│  ○ 完整对话                    │
├────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐   │
│  │ Markdown │  │   PDF    │   │
│  │  .md     │  │  .pdf    │   │
│  └──────────┘  └──────────┘   │
│  ┌──────────┐                  │
│  │   Word   │                  │
│  │  .docx   │                  │
│  └──────────┘                  │
└────────────────────────────────┘
```

- 背景蒙层：`bg-black/50`，`backdrop-blur-sm`
- 弹窗：`bg-surface border border-subtle`，圆角 20px，宽 320px
- 出现动画：scale `0.95 → 1.0` + fade，`200ms ease-out`
- 格式卡片：`border border-subtle hover:border-accent hover:bg-accent-subtle`，圆角 12px，过渡 150ms

**PDF 导出 Loading 状态**：点击 PDF 卡片后，卡片内图标替换为旋转 Loader，文字变为"生成中..."，禁止重复点击，完成后恢复并触发下载。预计生成时间 1-3 秒。

---

## 十一、交互状态总表

| 状态 | 视觉表现 |
|------|----------|
| 默认 | 正常颜色 |
| Hover | `bg-elevated`，`transition 150ms` |
| Active/按下 | `scale-[0.97]`，`transition 100ms` |
| Focus | `outline: 2px solid #2563EB; outline-offset: 2px` |
| Disabled | `opacity-40`，`cursor-not-allowed` |
| 选中（文件） | `bg-accent-subtle border-accent-border` |
| 流式进行中 | 输入框 disabled，发送按钮 Loader 状态 |
| 错误 | 红色文字/图标，状态色 `#EF4444` |

---

## 十二、动画规范

| 场景 | 时长 | 缓动 |
|------|------|------|
| hover 变色 | 150ms | ease |
| 按下反馈 | 100ms | ease-out |
| 侧栏折叠 | 250ms | ease-in-out |
| 步骤面板出现 | 200ms | ease-out |
| 步骤面板收起 | 200ms | ease-in |
| 弹窗出现 | 200ms | ease-out |
| 弹窗关闭 | 150ms | ease-in |
| 上下文提示条 | 150ms | ease-out |
| 文件列表操作按钮 | 150ms | ease |
| 流式光标闪烁 | 800ms | step-end（无缓动） |

---

## 十三、组件尺寸速查

| 组件 | 规格 |
|------|------|
| Header 高度 | 48px |
| 侧栏宽度 | 272px |
| 侧栏折叠宽度 | 0px |
| UploadZone 高度 | 约 72px |
| FileItem 高度 | 约 52px |
| 输入框最小高度 | 44px |
| 发送/操作按钮 | 32×32px（最小触发区 44×44px） |
| 头像大小 | 32×32px |
| 消息气泡最大宽度 | 用户 75%，AI 85% |
| 步骤面板圆角 | 12px |
| 弹窗宽度 | 320px |
| 全局圆角基准 | 6px（small），10px（medium），16px（large），20px（pill/modal） |

---

## 十四、已知限制说明（界面需提示）

在以下场景，界面需要给用户友好提示：

| 场景 | 提示方式 | 文案 |
|------|----------|------|
| 扫描件 PDF 无文字 | 解析状态失败 + 错误原因 | 此 PDF 为扫描件，暂不支持识别 |
| 加密 PDF | 解析状态失败 + 错误原因 | 文件已加密，无法读取 |
| 文档超长（>8000 字） | 状态就绪 + 黄色截断提示 | 文档过长，已截取前 8000 字 |
| 多文件超 context 上限 | 上下文提示条显示警告图标 | 部分文件因长度限制未被读取 |
| 文件解析中被选入对话 | AI 回复中说明 | 「文件名」仍在解析中，本次对话暂时跳过 |
| 上传不支持的文件类型 | 上传区 toast 提示 | 不支持此文件格式，请上传 PDF/DOCX/图片 |
| 文件超出大小限制 | 上传区 toast 提示 | 文件超过 50MB 限制 |
| 图片无可识别文字 | 状态就绪，AI 提示 | 图片中未检测到文字 |
