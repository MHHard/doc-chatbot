# 系统数据流转 & 使用流程分析

**版本**：v1.0  
**日期**：2026-06-06

---

## 一、完整数据流转图

```
┌─────────────────────────────────────────────────────────────────────┐
│                           前端（Browser）                            │
│                                                                     │
│  ┌─────────────┐    ┌──────────────┐    ┌───────────────────────┐  │
│  │  FileStore  │    │  ChatStore   │    │       UI Components   │  │
│  │  (Zustand)  │    │  (Zustand)   │    │  FileManager / Chat   │  │
│  └──────┬──────┘    └──────┬───────┘    └───────────────────────┘  │
│         │                  │                                        │
└─────────┼──────────────────┼────────────────────────────────────────┘
          │ REST API          │ SSE / REST
          ▼                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         后端（Go + Fiber）                           │
│                                                                     │
│  ┌──────────────┐    ┌──────────────┐    ┌───────────────────────┐ │
│  │ FileHandler  │    │ ChatHandler  │    │     In-Memory Store   │ │
│  │              │    │              │    │  map[id]*FileInfo     │ │
│  └──────┬───────┘    └──────┬───────┘    └───────────────────────┘ │
│         │                  │                                        │
└─────────┼──────────────────┼────────────────────────────────────────┘
          │                  │
    ┌─────┴──────┐    ┌───────┴──────────────────┐
    │  本地磁盘   │    │      外部 API             │
    │  ./uploads │    │                          │
    └────────────┘    │  ┌─────────────────────┐ │
                      │  │  DashScope OCR API  │ │
                      │  │  (qwen-vl-ocr)      │ │
                      │  └─────────────────────┘ │
                      │  ┌─────────────────────┐ │
                      │  │   Qwen Chat API     │ │
                      │  │   (qwen-plus, SSE)  │ │
                      │  └─────────────────────┘ │
                      └──────────────────────────┘
```

---

## 二、核心流程逐步拆解

### 流程 1：文件上传 & 解析

```
用户选择/拖拽/粘贴文件
        │
        ▼
前端校验（类型、大小）─── 不通过 ──▶ Toast 提示，终止
        │ 通过
        ▼
POST /api/files/upload（multipart）
        │
        ▼
后端校验（二次校验类型、大小）
        │
        ▼
保存到 ./uploads/{uuid}.ext
        │
        ▼
写入 MemoryStore（parseStatus: "pending"）
        │
        ▼
立即返回 FileInfo 给前端（前端显示"解析中"）
        │
        ▼ （goroutine 异步执行）
  ┌─────┴──────────────────────────────────┐
  │          按文件类型分支                  │
  ├─ PDF  ──▶  pdfcpu 提取文字              │
  ├─ DOCX ──▶  ZIP+XML 解析提取段落         │
  └─ 图片 ──▶  base64 → DashScope OCR API  │
              ▼
        解析成功：写入 Content，parseStatus: "ready"
        解析失败：写入 parseError，parseStatus: "failed"
              │
              ▼
前端轮询 GET /api/files，感知状态变化，更新 UI
```

**轮询策略**：上传后每 2 秒轮询一次，就绪后停止；最多轮询 30 次（1 分钟超时）

---

### 流程 2：发起聊天

```
用户在输入框输入消息，点击发送
        │
        ▼
前端读取 ChatStore.selectedIds（勾选的文件）
        │
        ▼
POST /api/chat/stream
  body: { message, fileIds[], history[] }
        │
        ▼
后端遍历 fileIds，从 MemoryStore 取各文件
        │
  ┌─────┴──────────────────────────────────┐
  │          按文件状态分类                  │
  ├─ ready    ──▶ 拼入 Content 作为上下文   │
  ├─ pending  ──▶ 跳过，在 system prompt   │
  │               中注明"该文件解析中"       │
  └─ failed   ──▶ 跳过，在 system prompt   │
                  中注明"该文件解析失败"     │
        │
        ▼
组装 messages 数组：
  [system] + [文件内容] + [历史10条] + [用户消息]
        │
        ▼
调用 Qwen API（stream: true）
        │
        ▼
逐 chunk 读取 SSE，转发给前端：
  data: {"content": "xxx"}\n\n
        │
        ▼
前端 ReadableStream 接收，逐字 appendToLast
        │
        ▼
data: [DONE] → 前端标记 streaming: false，显示操作按钮
```

---

### 流程 3：导出

```
用户点击 AI 消息的「导出」按钮
        │
        ▼
弹出 ExportModal，选择范围（单条/全对话）+ 格式（MD/PDF/DOCX）
        │
        ▼
前端纯本地生成（无后端请求）：
  ├─ Markdown ──▶ Blob 下载 .md 文件
  ├─ PDF      ──▶ jsPDF 生成 .pdf
  └─ DOCX     ──▶ docx npm 包生成 .docx
        │
        ▼
浏览器触发文件下载，弹窗关闭
```

---

### 流程 4：重试

```
上传失败：
  用户点击「重新上传」
        │
  前端用缓存的 File 对象重发 POST /api/files/upload
  （若 File 对象丢失，弹出系统文件选择框）

解析失败：
  用户点击「重新解析」
        │
  POST /api/files/:id/reparse
        │
  后端重置 parseStatus: "pending"，重新执行解析 goroutine
        │
  返回更新后 FileInfo，前端更新 UI
```

---

## 三、边界情况分析

### 3.1 文件上传阶段

| 边界场景 | 当前处理 | 风险等级 |
|----------|----------|----------|
| 文件类型不支持（如 .xlsx） | 前端+后端双重校验，Toast 提示 | ✅ 已覆盖 |
| 文件超过 50MB | 前端+后端校验，提示超限 | ✅ 已覆盖 |
| 上传中断（网络断开） | 返回上传失败 + 错误原因 | ✅ 已覆盖 |
| 同一文件重复上传 | **未处理**：会存为两个独立文件 | ⚠️ 待决策 |
| 同时上传多个大文件 | 并发上传，各自独立状态 | ✅ 已覆盖 |
| 空文件（0 字节） | 后端校验文件大小 > 0，拒绝 | ⚠️ 需补充 |
| 有密码保护的 PDF | pdfcpu 无法解析，返回 parseError："文件已加密，无法读取" | ⚠️ 需补充 |
| 文件名含特殊字符/超长 | 后端用 uuid 命名存储，原始名入库，安全 | ✅ 已覆盖 |

### 3.2 文件解析阶段

| 边界场景 | 当前处理 | 风险等级 |
|----------|----------|----------|
| OCR 服务不可用 / 5xx | 返回 parseError："OCR 服务异常" + 重试按钮 | ✅ 已覆盖 |
| OCR 请求超时（>120s） | httpClient 设 120s 超时，超时返回 failed | ✅ 已覆盖 |
| 扫描件 PDF（无文字层） | pdfcpu 返回空文字 → parseError："此 PDF 为扫描件" | ✅ 已覆盖 |
| 图片没有任何文字 | OCR 返回空字符串 → Content 为空，parseStatus: "ready"，AI 会告知无内容 | ⚠️ 待优化 |
| DOCX 文件损坏 | XML 解析失败 → parseError："文件损坏，无法解析" | ✅ 已覆盖 |
| 文档内容超长（>10 万字） | **未处理**：会超出 Qwen 上下文窗口限制 | 🔴 需处理 |
| 解析 goroutine panic | recover 兜底，标记为 failed | ⚠️ 需补充 |
| 服务重启后 MemoryStore 清空 | Session 级设计，重启后需重新上传（已知限制） | ✅ 文档已说明 |

**文档超长的处理建议**：解析完成后若文字超过 8000 字，截取前 8000 字并在 parseError 附加提示："文档过长，仅读取前 8000 字"（parseStatus 仍为 ready）。

### 3.3 聊天阶段

| 边界场景 | 当前处理 | 风险等级 |
|----------|----------|----------|
| 流式输出中用户再次发送 | 发送按钮 disabled，不可重复提交 | ✅ 已覆盖 |
| 勾选文件都在解析中 | system prompt 中注明，AI 提示用户等待 | ✅ 已覆盖 |
| 未勾选任何文件 | 正常对话，无文件上下文 | ✅ 已覆盖 |
| Qwen API 不可用 / 5xx | SSE 返回 `{"error": "..."}` → 前端在消息末尾显示红色错误提示 | ✅ 已覆盖 |
| SSE 连接中断 | ReadableStream `done: true` 触发 onDone，消息不完整但不崩溃 | ⚠️ 可加重连 |
| 用户粘贴大段文字（≥300字）到输入框 | 检测到纯文字粘贴，滑出横幅询问"上传为文档 / 保留为文字"；选上传则创建 .txt 文件走正常上传流程 | ✅ 已覆盖 |
| 消息太长（保留为文字超 2000 字） | 前端截断并 Toast 提示 | ✅ 已覆盖 |
| 同时勾选大量大文件 | 可能超出 Qwen 上下文窗口 → 超限时 API 报错 | 🔴 需处理 |
| 历史消息太长 | 只取最近 10 条，有效控制 | ✅ 已覆盖 |
| 用户发送空消息 | 前端校验 trim() 不为空，send 按钮 disabled | ✅ 已覆盖 |

**多文件超 context 的处理建议**：后端统计所有已选文件 Content 总字数，若超过 30000 字，截取最大的几个文件直至不超限，并在 system prompt 中告知用户"部分文件内容因长度限制被截断"。

### 3.4 导出阶段

| 边界场景 | 当前处理 | 风险等级 |
|----------|----------|----------|
| 导出内容为空 | 导出按钮在消息内容为空时不出现 | ✅ 已覆盖 |
| PDF 中含中文字符 | jsPDF 默认不支持中文 → 导出为乱码 | 🔴 需处理 |
| 导出超长对话 | 无限制，jsPDF/docx 自动分页 | ✅ 基本覆盖 |
| 用户关闭页面中途下载 | 浏览器行为，无法干预 | — |

**PDF 中文问题建议**：使用前端 html2canvas 将渲染好的 Markdown 截图转 PDF，保留中文；或提示用户"PDF 导出不支持中文，建议使用 Markdown 或 Word 格式"。

### 3.5 会话 & 状态

| 边界场景 | 当前处理 | 风险等级 |
|----------|----------|----------|
| 用户刷新页面 | 聊天记录清空（session 级，已知设计） | ✅ 文档已说明 |
| 文件在对话中被删除 | 已勾选的 ID 从 selectedIds 移除，下次发消息不再引用 | ✅ 已覆盖 |
| 浏览器断网后恢复 | 流式请求中断，显示不完整消息 + 错误提示；重新联网可继续发新消息 | ⚠️ 体验可优化 |
| 多个 Tab 同时打开 | 各自独立 session，文件列表通过 GET /api/files 共享，聊天互不影响 | ✅ 可接受 |
| 长时间不操作（内存回收） | Go 进程内存中的 FileInfo 一直保留直到重启，无超时清理 | ⚠️ 生产需加 TTL |

---

## 四、需要在代码中补充的处理

基于以上分析，以下问题**在 PRD 已覆盖但代码实现时需特别注意**：

| 优先级 | 问题 | 处理方案 |
|--------|------|----------|
| 🔴 P0 | PDF 导出中文乱码 | 改用 html2canvas → jsPDF 方案 |
| 🔴 P0 | 多文件 context 超出 Qwen 上下文窗口 | 后端按总字数 30000 字截断，告知用户 |
| 🟡 P1 | 文档内容超长（>8000 字） | 解析时截取，状态仍为 ready，附注截断提示 |
| 🟡 P1 | 加密 PDF / 空文件 / panic recover | 后端补充相应错误码和 parseError 文案 |
| 🟡 P1 | 重复上传同一文件 | 可选：MD5 校验去重，或直接允许（当前方案） |
| 🟢 P2 | SSE 断线重连 | EventSource 重连或前端 fetch retry |
| 🟢 P2 | 图片 OCR 返回空内容 | parseStatus 标为 ready 但 Content 为空，AI 提示"图片无可识别文字" |
| 🟢 P2 | 输入框字数限制 | 前端限制 2000 字，超出提示 |
