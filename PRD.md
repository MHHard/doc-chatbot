# 文档处理 ChatBot — 产品需求文档（PRD）

**版本**：v1.0  
**日期**：2026-06-06  
**项目类型**：面试作业

---

## 一、项目概述

基于 React + Go 的文档处理 ChatBot，用户可以上传文件、管理文件列表，并就这些文件与 AI Agent 进行聊天交互。Agent 是一个文档总结助手，能够理解和分析上传的文档内容。

---

## 二、整体布局

参考 Claude Web 风格，分为左右两栏：

```
┌─────────────────────┬──────────────────────────────────────────┐
│     文件管理侧栏     │                  聊天区域                 │
│      (280px)        │                                          │
│                     │  ┌────────────────────────────────────┐  │
│  ┌───────────────┐  │  │           消息列表                  │  │
│  │   上传区域    │  │  │                                    │  │
│  └───────────────┘  │  │                                    │  │
│                     │  └────────────────────────────────────┘  │
│  文件列表           │  ┌────────────────────────────────────┐  │
│  ☑ file1.pdf  ···  │  │    已选 N 个文件 · 聊天输入框        │  │
│  ☐ file2.docx ···  │  └────────────────────────────────────┘  │
│  ☐ image.png  ···  │                                          │
└─────────────────────┴──────────────────────────────────────────┘
```

- 左侧侧栏可折叠/展开（顶部 Header 有切换按钮）
- 顶部 Header 展示应用名称和侧栏切换按钮

---

## 三、模块一：文件管理

### 3.1 文件上传

支持三种上传方式：

| 方式 | 描述 |
|------|------|
| 点击上传 | 上传区域点击，弹出系统文件选择框，支持多选 |
| 拖拽上传 | 将文件拖入左侧上传区域松开即上传 |
| 粘贴上传 | 从系统剪贴板粘贴文件/图片（Ctrl+V / Cmd+V），触发上传 |

**支持的文件类型**：PDF、DOCX、TXT、JPG、JPEG、PNG、WEBP  
**单文件大小限制**：50MB  
**上传反馈**：上传区域显示 Loading 动画，完成后文件出现在列表顶部

### 3.2 文件列表

每个文件项展示以下信息：

| 字段 | 说明 |
|------|------|
| 文件图标 | 根据文件类型显示不同颜色图标（PDF红色、DOCX蓝色、TXT灰色、图片紫色） |
| 文件名 | 截断显示，hover 可见完整名称 |
| 文件大小 | 自动换算 B / KB / MB |
| 上传时间 | 相对时间（如：3分钟前） |
| 解析状态 | 见 3.3 |
| 勾选框 | 左侧复选框，控制是否加入对话上下文 |

### 3.3 解析状态

文件上传成功后后台异步解析，前端通过轮询后端 `parseStatus` 字段感知进度。上传失败发生在文件成功入库之前，由前端以本地 `UploadTask` 状态展示，不进入后端文件列表。

| 状态 | 展示 | 颜色 |
|------|------|------|
| `pending` | 旋转图标 + "解析中..." | 黄色 |
| `ready` | 对勾图标 + "已就绪"；若有 `parseWarning`，在状态旁显示黄色提示 | 绿色 |
| `upload_failed` | 叹号图标 + "上传失败" + 错误原因；前端本地上传任务状态，不属于后端 `FileInfo.parseStatus` | 红色 |
| `failed` | 叹号图标 + "解析失败" + 错误原因 | 红色 |

> 未解析完成的文件可以被勾选，但 AI 回复中将注明"该文件仍在解析中，暂时跳过"。

### 3.3.1 错误原因展示

上传失败或解析失败时，文件名下方展示一行简短的错误原因文字（红色，11px）：

| 错误场景 | 展示文案 |
|----------|----------|
| 文件类型不支持 | 不支持此文件格式 |
| 文件超过大小限制 | 文件超过 50MB 限制 |
| 网络超时 / 上传中断 | 上传超时，请检查网络 |
| PDF / 图片 OCR 失败或服务不可用 | OCR 服务异常，请稍后重试 |
| 加密 / 有密码的 PDF | 文件已加密，无法读取 |
| 空文件（0 字节） | 文件内容为空 |
| DOCX 文件损坏 | 文件损坏，无法解析 |
| 其他未知错误 | 解析失败，请重试 |

解析失败时，后端在 FileInfo 中返回 `parseError` 字段，前端直接展示该字段内容。上传失败时，前端展示上传请求返回的错误原因。若文件已就绪但存在非阻塞提示（如内容被截断、图片无文字），后端返回 `parseWarning` 字段，前端以黄色小字展示。

### 3.3.2 重试操作

**上传失败**时，文件项下方出现 **「重新上传」** 按钮：
- 点击后重新触发上传流程（前端用本地缓存的 File 对象重新请求 `/api/files/upload`）
- 重试期间按钮变为 Loading 状态，不可重复点击
- 若 File 对象已丢失（例如浏览器未保留粘贴/拖拽来源），按钮文案改为 **「选择文件重试」**，点击后弹出文件选择框；页面刷新后本地上传失败任务不保留

**解析失败**时，文件项下方出现 **「重新解析」** 按钮：
- 点击后调用 `POST /api/files/:id/reparse`，触发后端重新执行解析
- 状态立即切换回"解析中..."，完成后更新为最新状态
- 重试期间按钮隐藏，解析中状态显示

**按钮样式**：
- 尺寸：小型，`px-2.5 py-1`，圆角 6px
- 上传失败：`border border-red-800/50 text-red-400 hover:bg-red-900/20`
- 解析失败：`border border-yellow-800/50 text-yellow-500 hover:bg-yellow-900/20`
- 字号：11px，与状态文字同行或换行显示

### 3.4 文件操作

每个文件项 hover 时右侧出现操作按钮：

| 操作 | 说明 |
|------|------|
| 重命名 | 点击后文件名变为可编辑 Input，Enter 确认，Escape 取消，自动保留后缀 |
| 下载 | 下载原始文件 |
| 删除 | 删除文件，同时从勾选列表移除，弹出确认提示 |

### 3.5 全选 / 取消全选

文件列表顶部展示已选数量，右侧有"全选 / 取消全选"切换按钮。

---

## 四、模块二：聊天界面

### 4.1 消息输入框

位于聊天区域底部，包含：

- **文本输入区**：多行自适应高度（最大 6 行），Enter 发送，Shift+Enter 换行
- **附件上传按钮**：点击弹出文件选择，上传后文件出现在左侧文件列表并自动勾选
- **拖拽上传**：将文件拖入聊天输入框区域即触发上传，同上
- **粘贴上传**：在输入框中 Cmd/Ctrl+V 粘贴文件/图片，触发上传；粘贴纯文字时见 4.1.2
- **发送按钮**：流式输出时显示 Loading 图标，点击无效（不支持打断）

输入框上方显示当前上下文提示条：`已选 N 个文件作为上下文`（selectedIds > 0 时显示）

### 4.1.1 聊天框上传后的发送策略（混合方案）

通过聊天输入框上传文件后，采用**基于 3 秒阈值的混合策略**：

```
上传文件
    │
    ▼
发送按钮禁用，显示"解析中..."，开始计时
    │
    ├── 3 秒内解析完成 ──▶ 发送按钮恢复正常，文件内容完整参与对话（方案 A）
    │
    └── 超过 3 秒仍未完成 ──▶ 发送按钮恢复正常（方案 B）
              │
              ▼
         上下文提示条变为警告态：
         ⚠ 「文件名」解析中，发送后将跳过
              │
         用户可选择等待或直接发送
              │
         解析完成后弹出 Toast：
         「文件名」解析完成，可重新发送
```

**各文件类型的预期触发策略**：

| 文件类型 | 预期解析时间 | 触发策略 |
|----------|-------------|----------|
| DOCX / TXT（本地解析） | 100–300ms | 方案 A，用户几乎无感 |
| PDF / 图片（OCR 网络调用） | 5–15s | 方案 B，放行并警告 |
| 大体量 PDF | 10–30s | 方案 B，放行并警告 |

**前端实现要点**：
- 上传成功后立即开始轮询该文件状态（每 1 秒一次）
- 3 秒计时器与轮询并行运行，哪个条件先触发则执行对应逻辑
- 文件解析完成的 Toast 通知只在方案 B（放行后）才展示

### 4.1.2 输入框字数与大段文字处理

**输入框无硬性字数上限**，改为动态提示引导用户走正确路径：

| 输入字数 | 表现 |
|----------|------|
| < 1000 字 | 正常，无提示 |
| 1000–3000 字 | 输入框右下角字数变**黄色**，提示"内容较长，建议上传为文档" |
| > 3000 字 | 字数变**红色**，提示"超出建议长度，可能影响回复质量" |

> 超出建议长度时用户仍可继续输入和发送，系统不强制截断。

**粘贴大段文字的触发条件**：`paste` 事件 + 粘贴内容为纯文字（非文件） + 字符数 **≥ 1000**。

**交互流程**：

```
用户粘贴大段文字（≥ 1000 字）
        │
        ▼
文字正常出现在输入框（不打断用户）
输入框下方滑出一条提示横幅：
┌─────────────────────────────────────────────┐
│ 📄 检测到大段文本（1,234 字）               │
│    作为文档上传，可让 AI 更好地分析          │
│                  [上传为文档]  [保留为文字] │
└─────────────────────────────────────────────┘
        │
   ┌────┴──────────────────────────┐
   │                               │
用户点「上传为文档」          用户点「保留为文字」
   │                           或 10s 后自动关闭
   ▼                               │
以粘贴内容创建 .txt 文件            ▼
文件名：粘贴文本_HH:MM.txt     横幅消失，输入框保留文字
POST /api/files/upload
文件出现在侧栏，自动勾选
输入框清空，光标就位
等待用户输入问题
```

**提示横幅规格**：
- 位置：输入框与上下文提示条之间，`slideDown 150ms` 出现
- 背景：`bg-accent-subtle border border-accent-border`，圆角 10px
- 超时：10 秒无操作自动关闭（等同"保留为文字"）
- 不阻塞用户：横幅出现时仍可继续编辑输入框或直接发送

**后端**：`.txt` 文件直接读取文件内容，无需 OCR，解析 < 100ms，必然触发 4.1.1 中的方案 A。

### 4.2 消息气泡

**用户消息**：右对齐，蓝色气泡，纯文本展示  
**AI 消息**：左对齐，深色气泡，Markdown 渲染

AI 消息 Markdown 渲染支持：
- 标题（h1-h3）
- 粗体、斜体、删除线
- 有序/无序列表
- 代码块（带语法高亮）
- 行内代码
- 引用块
- 表格
- 分割线

### 4.3 流式渲染

AI 响应通过 SSE（Server-Sent Events）流式返回，前端逐字追加到消息气泡末尾。  
流式输出过程中，消息末尾显示闪烁光标。

### 4.4 消息操作

AI 消息 hover 时气泡下方出现操作按钮（流式输出完成后）：

| 操作 | 说明 |
|------|------|
| 复制 | 复制消息 Markdown 原文，点击后变为"已复制" |
| 导出 | MVP 支持 Markdown 导出；PDF / Word 导出为 P1 增强功能（见 4.5） |

### 4.5 导出弹窗

MVP 阶段点击"导出"默认下载 Markdown（.md）文件。P1 阶段升级为导出 Modal，包含：

**导出范围选择**（Radio）：
- 仅此条消息
- 完整对话

**导出格式选择**（三个卡片按钮）：
- Markdown（.md）
- PDF（.pdf）
- Word（.docx）

点击格式卡片后立即触发下载并关闭弹窗。

**各格式实现方案**：

| 格式 | 实现方式 | 说明 |
|------|----------|------|
| Markdown | `Blob` 直接下载 | 无损，保留原始 Markdown 语法 |
| PDF（P1） | `html2canvas` 截图 → `jsPDF` 嵌入图片 | 解决中文字符渲染问题；样式与界面一致 |
| DOCX（P1） | `docx` npm 包构建文档结构 | 支持中文，段落/标题/表格正确映射 |

> **PDF 说明**：使用 html2canvas 将已渲染的 Markdown 区域截图后嵌入 PDF，完整保留中文和排版，无需内嵌字体文件。生成时展示 Loading 状态，完成后自动下载。

### 4.6 空状态

对话未开始时，聊天区域中央展示欢迎引导页：
- 助手图标
- 标题"文档分析助手"
- 提示文字：上传文件并勾选，然后发送消息

### 4.7 清空对话

顶部工具栏（消息列表右上角）有"清空对话"按钮，清空所有消息（文件列表不受影响）。

---

## 五、AI Agent 能力

### 5.1 两个 API 的分工

| API | Key | 职责 |
|-----|-----|------|
| DashScope OCR（多模态） | `OCR_API_KEY` | 文件预处理：将 PDF / 图片识别为文字 |
| 模型百炼 Qwen | `QWEN_API_KEY` | 对话推理：基于提取的文字回答用户问题 |

整体数据流：
```
用户上传文件
  ├─ PDF          →  按页转图片 → DashScope OCR API  →  提取文字  →  存入内存
  ├─ 图片          →  DashScope OCR API              →  提取文字  →  存入内存
  ├─ DOCX         →  本地 XML 解析        →  提取文字  →  存入内存
  └─ TXT          →  本地直接读取文本      →  存入内存

用户发送消息
  └→  [系统提示] + [勾选文件的文字内容] + [历史对话(最近10条)] + [用户消息]
       └→  Qwen API (stream=true)  →  SSE  →  前端流式渲染
```

### 5.2 文档解析策略

| 文件类型 | 解析方式 | 说明 |
|----------|----------|------|
| PDF | 先按页转图片，再调用 OCR API（`qwen-vl-ocr` 模型） | `qwen-vl-ocr` 输入为图片 URL / Base64 Data URL；PDF 需先渲染为多张高质量图片后逐页识别，统一覆盖文字型和扫描件 PDF |
| JPG / PNG / WEBP | OCR API（`qwen-vl-ocr` 模型） | 图片转 base64 Data URL 后调用，返回识别文字 |
| DOCX | Go 本地 XML 解析 | DOCX 本质是 ZIP+XML，直接解压提取段落文字 |
| TXT | Go 本地读取文本 | 用于粘贴大段文字生成的 `.txt` 文件和用户直接上传的文本文件 |

**OCR 调用方式**（OpenAI 兼容端点）：

```
POST {OCR_API_HOST}/chat/completions
Authorization: Bearer {OCR_API_KEY}

{
  "model": "qwen-vl-ocr",
  "messages": [{
    "role": "user",
    "content": [
      { "type": "image_url", "image_url": { "url": "data:image/jpeg;base64,{base64}" } },
      { "type": "text", "text": "请提取图片中的所有文字内容，保持原有格式。" }
    ]
  }]
}
```

解析在文件上传后**异步执行**，结果存入服务内存，前端通过解析状态字段感知进度。

> PDF 处理说明：根据 DashScope Qwen-OCR 官方文档，处理多页 PDF 时应先使用图像处理库将 PDF 按页转换为高质量图片，再参照多图像输入逐页识别；不要直接把 PDF 文件作为 `image_url` 的 Base64 Data URL 传入。

**文档长度截断**：解析完成后，若提取的文字超过 **8000 字**，截取前 8000 字存储，`parseStatus` 仍为 `ready`，`parseWarning` 附注提示：`"文档过长，已截取前 8000 字"`。前端在解析状态徽章旁以黄色小字展示该提示。

**边界错误处理**（后端必须覆盖）：

| 错误场景 | parseStatus | 返回字段 / 文案 |
|----------|-------------|-----------------|
| PDF OCR 失败 / 超时 | `failed` | OCR 服务异常，请稍后重试 |
| 加密 / 有密码的 PDF | `failed` | 文件已加密，无法读取 |
| 空文件（0 字节） | `failed` | 文件内容为空 |
| DOCX XML 损坏 | `failed` | 文件损坏，无法解析 |
| 图片 OCR 失败 / 超时 | `failed` | OCR 服务异常，请稍后重试 |
| OCR 返回空内容（图片无文字） | `ready` | `parseWarning`: 图片中未检测到文字 |
| goroutine panic（兜底） | `failed` | 解析失败，请重试 |

> 后端所有解析 goroutine 必须用 `recover()` 兜底，任何 panic 都转换为 `failed` 状态而非服务崩溃。

### 5.3 对话模型

统一使用 `qwen-plus`，所有文件内容已在预处理阶段转为纯文字，直接拼入 prompt，无需切换模型。

### 5.4 系统提示词

```
你是一个专业的文档分析助手，擅长总结、分析和整理文档内容。
请用中文回复，使用 Markdown 格式，结构清晰，重点突出。
```

### 5.5 上下文拼接规则

每次请求按如下顺序组装 messages：

```
[system]  系统提示词 + 文件状态注释

[user]    ### 文件：report.pdf
          <OCR 提取的 PDF 文字内容（最多 8000 字）>

          ### 文件：image.png
          <OCR 提取的文字内容>

          ### 文件：contract.docx
          <本地解析的文字内容>

[user]    <历史第1条用户消息>
[assistant] <历史第1条 AI 回复>
...（最近 10 条）

[user]    <当前用户消息>
```

**文件状态处理规则**：

| 文件状态 | 处理方式 |
|----------|----------|
| `ready` | 正常拼入 Content |
| `ready`（有 `parseWarning`） | 拼入 Content，system 中附注对应提示 |
| `pending` | 跳过，system 中注明"[文件名] 正在解析，本次跳过" |
| `failed` | 跳过，system 中注明"[文件名] 解析失败，本次跳过" |

**多文件 Context 总量控制**：

后端在拼接前统计所有 `ready` 文件的文字总长度：
- 总长度 ≤ 30000 字：全部拼入
- 总长度 > 30000 字：按文件大小从小到大依次纳入，直至接近上限为止；被跳过的文件在 system 中注明"[文件名] 因上下文长度限制被跳过"
- 前端在"已选 N 个文件"提示条旁展示警告图标，hover 显示"部分文件因长度限制未被读取"

---

## 六、API 接口设计

### 6.1 文件接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/files/upload` | 上传文件，返回 FileInfo |
| GET | `/api/files` | 获取文件列表 |
| PATCH | `/api/files/:id/rename` | 重命名文件 |
| DELETE | `/api/files/:id` | 删除文件 |
| GET | `/api/files/:id/download` | 下载原始文件 |
| POST | `/api/files/:id/reparse` | 重新触发解析，返回更新后的 FileInfo |

**FileInfo 数据结构**：
```json
{
  "id": "uuid",
  "name": "report.pdf",
  "originalName": "report.pdf",
  "size": 102400,
  "mimeType": "application/pdf",
  "uploadedAt": "2026-06-06T10:00:00Z",
  "parseStatus": "ready",     // pending | ready | failed
  "parseError": null,         // 仅 failed 时存在
  "parseWarning": "文档过长，已截取前 8000 字"  // ready 状态下的非阻塞提示，可为空
}
```

### 6.2 聊天接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/chat/stream` | SSE 流式聊天 |

**请求体**：
```json
{
  "message": "帮我总结这份文档",
  "fileIds": ["uuid1", "uuid2"],
  "history": [
    { "role": "user", "content": "..." },
    { "role": "assistant", "content": "..." }
  ]
}
```

**SSE 响应格式**：
```
data: {"content": "这份"}
data: {"content": "文档"}
data: {"content": "主要讲述了..."}
data: [DONE]
```

---

## 七、技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | React 18 + TypeScript + Vite |
| 样式 | Tailwind CSS |
| 状态管理 | Zustand |
| Markdown 渲染 | react-markdown + remark-gfm + rehype-highlight |
| 文件上传交互 | react-dropzone |
| 图标 | lucide-react |
| 导出：MD | Blob 下载 |
| 导出：PDF | P1 增强功能，MVP 可暂不实现；方案为 jsPDF |
| 导出：DOCX | P1 增强功能，MVP 可暂不实现；方案为 docx（npm） |
| 后端框架 | Go + Fiber v2 |
| 图片 OCR | DashScope OCR API（qwen-vl-ocr，OpenAI 兼容端点） |
| PDF 解析 | PDF 按页转图片 + DashScope OCR API（qwen-vl-ocr，OpenAI 兼容端点） |
| DOCX 解析 | Go 本地 XML 解析（标准库） |
| 流式输出 | SSE（Server-Sent Events） |
| 文件存储 | 本地磁盘（./uploads），服务启动时清空 |
| 运行时状态 | Go 内存（sync.Map），重启后清空 |

---

## 八、环境变量

```env
# 模型百炼 Qwen —— 对话推理
QWEN_API_KEY=        # sk-xxx
QWEN_API_HOST=       # OpenAI Compatible Base URL

# DashScope OCR —— PDF / 图片文字识别
OCR_API_KEY=         # sk-xxx
OCR_API_HOST=        # DashScope OCR API Base URL

# 公共
WORKSPACE_ID=        # 阿里云工作空间 ID
UPLOAD_DIR=./uploads
PORT=8080
```

---

## 九、非功能要求

- **文件大小限制**：单文件 ≤ 50MB
- **支持文件类型**：PDF、DOCX、TXT、JPG、JPEG、PNG、WEBP
- **聊天记录持久化**：Session 级别，刷新页面清空
- **多用户**：暂不支持，单用户演示
- **认证**：无
- **跨域**：后端开放 CORS，允许前端 localhost:5173 访问
- **存储生命周期**：文件存储与运行时状态均为 Session 级，服务启动时清空 `./uploads` 目录并重置内存 Store，防止磁盘孤儿文件堆积
- **系统依赖**：ECS 需安装 `poppler-utils`（提供 `pdftoppm` 命令）用于 PDF 逐页渲染；`sudo apt install poppler-utils`

### 启动清理逻辑（后端）

```
服务启动时（main.go）：
  1. 检查 UPLOAD_DIR 是否存在
  2. 若存在，删除目录下所有文件（不删目录本身）
  3. 重新创建空目录（os.MkdirAll）
  4. 初始化空 MemoryStore
  5. 开始监听端口
```

> 清理操作在服务开始接受请求**之前**完成，不影响正在处理的请求（因为此时还没有请求）。

---

## 十、目录结构

```
doc-chatbot/
├── backend/
│   ├── main.go
│   ├── go.mod
│   ├── .env.example
│   ├── config/config.go
│   ├── models/models.go
│   ├── store/store.go
│   ├── services/
│   │   ├── pdf.go      # PDF → 逐页渲染图片（pdftoppm）→ base64 → OCR
│   │   ├── ocr.go      # DashScope OCR API（图片 base64 → 文字）
│   │   ├── docx.go     # 本地 DOCX XML 解析 + TXT 文件直接读取
│   │   └── qwen.go     # Qwen 对话 API（SSE 流式）
│   └── handlers/
│       ├── files.go
│       └── chat.go
└── frontend/
    ├── package.json
    ├── vite.config.ts
    ├── tailwind.config.js
    └── src/
        ├── App.tsx
        ├── main.tsx
        ├── index.css
        ├── types/index.ts
        ├── api/
        │   ├── files.ts
        │   └── chat.ts
        ├── store/
        │   ├── fileStore.ts
        │   └── chatStore.ts
        ├── utils/
        │   └── export.ts
        └── components/
            ├── layout/AppLayout.tsx
            ├── files/
            │   ├── FileManager.tsx
            │   ├── UploadZone.tsx
            │   └── FileItem.tsx
            └── chat/
                ├── ChatPanel.tsx
                ├── MessageList.tsx
                ├── MessageBubble.tsx
                ├── ChatInput.tsx
                └── ExportModal.tsx
```
